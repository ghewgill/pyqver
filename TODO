- specific functions within modules
    - check "from module import name" syntax as well as "module.name"
- identify syntax changes, eg. "except Exception as x:" or "f(a, *b, k=c)" (2.6)
- recognise decorators on classes (2.6)
